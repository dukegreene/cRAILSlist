<h1>Welcome to cRAILSlist!</h1>

<table>
  <tr>
    <th>Title</th>
    <th colspan="2"></th>
  </tr>
  <% @categories.each do |category| %>
  <tr>
    <td><%= link_to category.name, category %></td>
    <td><%= link_to 'Delete Category', category_path(category), method: :delete, data: { confirm: 'You serious?' } %></td>
  </tr>
  <% end %>
</table>
<br>
<%= link_to 'New category', new_category_path %>
